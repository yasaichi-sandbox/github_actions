name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Print git status
      run: |
        git symbolic-ref --short HEAD
        git branch -a
        git --no-pager log
